/**
 * @minute-one/voice-mock — offline adapter for tests and explicit demo mode.
 *
 * Never selected automatically. A real-provider failure surfaces as a failure; only a
 * deliberate user choice reaches this package.
 */
export * from "./mock";
